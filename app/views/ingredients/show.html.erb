<div id='ingredient'>
<h2><%= @ingredient.title %></h2>
<%= simple_format(@ingredient.description) %>
</div>

<%= link_to "Recipe Home", recipes_path(@recipe) %>

<%= link_to "Edit Ingredient", [:edit, @recipe, @ingredient] %>

<%= link_to "Delete Ingredient", [@recipe, @ingredient], :method => :delete,
:confirm => "Are you sure you want to delete this ingredient?" %>
